import { BizContext } from '../context/BizContext';
export declare class StatisticRecord {
    static readonly ET_NET = "net";
    static readonly ET_BIZ = "biz";
    static readonly KEY_TRADE_PARTNER = "partner";
    static readonly KEY_TRADE_OUT_TRADE_NO = "out_trade_no";
    static readonly KEY_TRADE_NO = "trade_no";
    static readonly KEY_BIZ_CONTENT = "biz_content";
    static readonly KEY_APP_ID = "app_id";
    private mTime;
    private mTrade;
    private mApp;
    private mSDK;
    private mIds;
    private mDevice;
    private mResult;
    private mBehavior;
    private mException;
    private mTemplate;
    private mBizContext;
    constructor(t40: BizContext, u40: boolean);
    putError(q40: string, r40: string, s40: Error | undefined): void;
    putErrorWithRes(m40: string, n40: string, o40: Error | undefined, p40: string): void;
    putErrorInner(j40: string, k40: string, l40: string): void;
    putErrorWithMsg(g40: string, h40: string, i40: string): void;
    putActionWithDetail(d40: string, e40: string, f40: string): void;
    putAction(b40: string, c40: string): void;
    putActionInner(y39: string, z39: string, a40: string): void;
    genEventTime(): string;
    private filter;
    private filterField;
    private getStackTraceMessage;
    format(x39: string): string;
    private buildTime;
    private randomUuid;
    private buildTrade;
    private buildApp;
    private buildSDK;
    private buildIds;
    private buildDevice;
}
